# Quest 24: 連鎖の極意

## 導入テキスト

ウホホホ！お前の連鎖魔法は相当なレベルに達した！

最後の試練だ。
複数のフックを連鎖させ、
条件分岐やエラーハンドリングを含む高度な設定を作れ。

これができれば、お前は「連鎖の魔術師」だ！

## 目標

- 高度なフック設定を理解する
- 複数フックの連鎖、条件分岐、エラーハンドリングを実装

## 成功条件

- [ ] 複数のフックを連鎖させる設定を作成した
- [ ] フックのベストプラクティス（軽量化、エラー処理など）を説明できる

## 報酬

- base_bp: 700
- first_clear_bonus: 140
- badge: "連鎖の魔術師"
- badge_icon: "🔗"
- badge_description: "Hooksを完全に習得した証"

## ヒント

### hint_1
複数フックの連鎖：
```json
{
  "matcher": "Write",
  "hooks": [
    { "type": "command", "command": "echo 'Step 1'" },
    { "type": "command", "command": "echo 'Step 2'" },
    { "type": "command", "command": "echo 'Step 3'" }
  ]
}
```

### hint_2
ベストプラクティス：
1. 軽量に保つ: 重い処理は避ける
2. エラーハンドリング: `command || true` で失敗を許容
3. 非同期処理: 長い処理は `nohup ... &` で
4. ログを残す: デバッグ用にログ出力

### hint_3
高度な設定例：

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash(git push*)",
        "hooks": [
          {
            "type": "command",
            "command": "echo '🔍 Push前チェック開始...'"
          },
          {
            "type": "command",
            "command": "git status --porcelain | grep -q . && echo '⚠️ 未コミットの変更があります' || echo '✅ クリーン'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": ["Write", "Edit"],
        "hooks": [
          {
            "type": "command",
            "command": "echo \"$(date '+%H:%M:%S'): $TOOL_NAME\" >> ~/.claude/activity.log || true"
          }
        ]
      },
      {
        "matcher": "Write(*.test.js)",
        "hooks": [
          {
            "type": "command",
            "command": "echo '🧪 テストファイルが更新されました。テストを実行することを推奨します。'"
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "echo '👋 セッション終了: '$(date) >> ~/.claude/sessions.log"
          }
        ]
      }
    ]
  }
}
```

## 関連リファレンス

docs/reference/04_hooks.md

# Quest 23: 実戦の連鎖

## 導入テキスト

ウホホ！matcher を使いこなせるようになったな。

次は「実戦の連鎖」を設定する。
日々の開発で本当に役立つフックを作ってみろ！

## 目標

- 実用的なフックを設定する
- 開発ワークフローを自動化する

## 成功条件

- [ ] 実用的なフックを1つ以上設定した
- [ ] そのフックが開発を効率化することを確認した

## 報酬

- base_bp: 650
- first_clear_bonus: 130

## ヒント

### hint_1
実用的なフックの例：
- ファイル変更後に自動フォーマット
- テストファイル更新後に自動テスト
- コミット前の確認

### hint_2
JS/TSファイル保存後に自動フォーマット：
```json
{
  "matcher": "Write(*.ts)",
  "hooks": [
    {
      "type": "command",
      "command": "prettier --write $TOOL_INPUT || true"
    }
  ]
}
```

### hint_3
practice/hook-cave/.claude/settings.json の実用的な設定例：

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash(rm -rf*)",
        "hooks": [
          {
            "type": "command",
            "command": "echo '🚨 危険！rm -rf コマンドを検知しました！'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write(*.js)",
        "hooks": [
          {
            "type": "command",
            "command": "echo '📝 JSファイルが作成されました: '$TOOL_INPUT"
          }
        ]
      },
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"$(date '+%Y-%m-%d %H:%M:%S'): ファイル編集\" >> ~/.claude-activity.log"
          }
        ]
      }
    ]
  }
}
```

## 関連リファレンス

docs/reference/04_hooks.md

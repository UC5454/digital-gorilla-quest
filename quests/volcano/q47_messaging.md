# Quest 47: メッセージング

## 🦍 導入

ウホッ！エージェントを配備できるようになったな。
だが、チームを動かすには「通信」が不可欠だ。

SendMessageでDM。broadcastで全体通知。
shutdown_requestで撤退命令。TeamDeleteで解散。
チームの通信と後始末——これが指揮官の仕事だ！

## 🎯 目標

- [ ] SendMessageで特定のエージェントにDMを送る
- [ ] broadcastで全エージェントに一斉通知する
- [ ] shutdown_requestでエージェントを終了させる

## 📋 やること

1. Agent Teamsでチームを作成し、2つのエージェントをspawnする
2. SendMessage（type: "message"）でDMを送る
3. SendMessage（type: "broadcast"）で全体通知を送る
4. SendMessage（type: "shutdown_request"）でエージェントを終了させる
5. 学んだことを `practice/boss-challenge/volcano/messaging-guide.md` にまとめる

## 📎 コピペ

```
Agent Teamsのメッセージング機能を実践して、結果を practice/boss-challenge/volcano/messaging-guide.md にまとめて：

1. チームを作って2つのエージェントをspawn
2. SendMessage type="message" で1体にDMを送る
3. SendMessage type="broadcast" で全体に通知を送る
4. 作業完了後、shutdown_request で各エージェントを終了
5. 各メッセージタイプの使い方と注意点をまとめる

特に「broadcastはコストが高いので慎重に使う」点を強調して。
```

## ✅ 成功条件

- [ ] DM（type: "message"）の仕組みを理解した
- [ ] broadcast（type: "broadcast"）の仕組みと注意点を理解した
- [ ] shutdown_requestの使い方を理解した
- [ ] `practice/boss-challenge/volcano/messaging-guide.md` が存在する

## 📦 成果物

- `practice/boss-challenge/volcano/messaging-guide.md`

## 🍌 報酬

- base_bp: 200
- first_clear_bonus: 40

## 💡 ヒント

### hint_1
メッセージタイプの使い分け:
- `message`: 1対1のDM。通常のコミュニケーションに使う
- `broadcast`: 全員への一斉送信。緊急時のみ使う
- `shutdown_request`: エージェント終了要求

### hint_2
broadcastは全エージェントに個別にメッセージを送るため、チームの人数分のAPIコールが発生する。「全員に伝える必要がある緊急事態」以外は、個別のmessageを使おう。

### hint_3
メッセージングの全体フロー:
```
1. DM送信
   SendMessage type="message" recipient="agent-name" content="調査お願い" summary="調査依頼"

2. 全体通知
   SendMessage type="broadcast" content="全タスク完了です" summary="完了通知"

3. 終了要求
   SendMessage type="shutdown_request" recipient="agent-name" content="作業完了"

4. チーム解散
   TeamDelete（全エージェント終了後に実行）
```

注意: shutdown_requestを送ると、エージェント側でapprove/rejectを判断する。まだ作業中なら拒否される場合もある。

## 📚 学習ポイント

- Agent Teams内の通信はSendMessageツールで行う
- DM（message）は日常的、broadcast は緊急時のみ使う
- shutdown_requestで行儀よくエージェントを終了させ、TeamDeleteで後始末する

## 📖 関連リファレンス

docs/reference/09_agent_teams.md
